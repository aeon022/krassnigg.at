---
// Dateipfad: frontend/src/pages/index.astro
import BaseLayout from "../layouts/BaseLayout.astro";
import { getEntry, getCollection } from "astro:content";
import Hero from "../components/blocks/Hero.astro";
import Expertise from "../components/blocks/Expertise.astro";
import Contact from "../components/blocks/Contact.astro";

const profile = await getEntry("settings", "profile");
const expertiseList = await getCollection("expertise");
const sortedExpertise = expertiseList.sort((a, b) => (a.data.order || 0) - (b.data.order || 0));
---

<BaseLayout title={`Dr. Helmut Krassnigg | ${profile?.data.title}`}>
  <main class="max-w-screen-xl mx-auto px-6 md:px-12">
    <Hero 
      name={profile?.data.name} 
      title={profile?.data.title} 
      bio={profile?.data.bio}
      heroImage={profile?.data.heroImage} 
    />
    
    <section id="expertise" class="py-32 border-t border-kyoto-accent/10">
      <div class="flex flex-col md:flex-row justify-between mb-20">
        <h2 class="text-4xl font-serif italic mb-6 md:mb-0">Expertise &<br/>Tätigkeitsbereiche</h2>
        <p class="max-w-sm text-kyoto-sub text-lg leading-relaxed">
          Strukturierte Begleitung und Prozessoptimierung für nachhaltige unternehmerische Resilienz.
        </p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-x-20 gap-y-16">
        {sortedExpertise.map((item) => (
          <Expertise title={item.data.title} description={item.data.description} />
        ))}
      </div>
    </section>

    <Contact 
      email={profile?.data.contactEmail} 
      location={profile?.data.location} 
    />
  </main>
</BaseLayout>